# Video Converter Configuration
# Copy this file to .env and customize the values

# Required: Directory containing video files to process
CONVERT_DIRECTORY=S:\Recent\AAC

# Optional: Maximum number of parallel jobs (default: 3)
CONVERT_MAX_PARALLEL=3

# Optional: Local temp directory for faster processing of network files
# CONVERT_TEMP_DIRECTORY=D:\temp

# Optional: Enable hardware acceleration (true/false, default: false)
CONVERT_USE_HW_ACCEL=false

# Optional: Hardware acceleration type (auto/nvenc/qsv/amf, default: auto)
# nvenc = NVIDIA GPU
# qsv = Intel QuickSync
# amf = AMD GPU
CONVERT_HW_ACCEL_TYPE=auto

# Optional: Comma-separated list of language codes to keep
# Default: eng,en,por,pt,english,portuguese (English and Portuguese)
# Also keeps unknown/undefined language tracks
# Examples: eng,spa,fre or en,es,fr
# CONVERT_LANGUAGES=eng,por
